# PROGRESS

Рабочее состояние проекта. Ведёт оркестратор, обновляет после каждого блока.
Это источник правды вместо истории диалога — при потере контекста читать отсюда.

## Где мы сейчас

| Этап | Состояние |
|---|---|
| **M1** — мост ConPTY ↔ WebView2 | **закрыт**, принят пользователем, смержен в `main` (`ef4d132`) |
| **M2** — проекты и вкладки | **закрыт**, принят «в целом гуд», три дефекта приёмки исправлены и прошли ревью |
| **M3** — история и `--resume` | **отложен пользователем**: «выбор и восстановление сессий пока не так важно» |
| **M4** — состояние сессий | **в работе**: Sessions и Terminal готовы, интерфейс состояний — нет |
| **M5** — доводка | **не начат**, кроме портов и попутно закрытых краевых случаев |

Сборка 0 ошибок / 0 предупреждений, тесты **313 зелёных**.
Ветка: `stage/m2-tabs`. Репозиторий: https://github.com/k-kuzmin/claude-agents-shell (public).

**Слитые ветки удаляются сразу** — локально и на remote, это часть процедуры мержа.

## Окружение (проверено)

| Что | Факт |
|---|---|
| .NET SDK | 9.0.317; рантаймы 8.0.30 и WindowsDesktop 8.0.30 → целимся в `net8.0` |
| WebView2 Runtime | 153.0.4234.32 |
| `pwsh` | **не найден в PATH** → реально работает `powershell.exe` 5.1 с его PSReadLine |
| node / npm | 24.13.0 / 11.6.2 |
| каталог Claude Code | `~/.claude/projects/D--Portfolio-Projects-claude-agents-shell` — подтверждает правило slug |

Страница требует `npm install && npm run vendor` в каталоге `web` — `web/vendor/` в `.gitignore`.

---

# Что сделано

## M1 — мост ConPTY ↔ WebView2 (закрыт)

Одно окно, один WebView2, страница с xterm.js, вкладка в ConPTY. Три раунда ревью и аудита.

### Дорого доставшееся знание

**`STARTF_USESTDHANDLES`.** Без флага `CreateProcess` протаскивает дочернему процессу стандартные
хэндлы **родителя**, и оболочка пишет туда, а не в псевдоконсоль: в пайп приходили только 16 байт
преамбулы ConPTY. `bImplicitHandles` ни при чём. Запуск из терминала давал пустой терминал — то есть
приёмка прошла бы случайно, только из проводника.

**`Ctrl+V` гасит сам xterm.js:** `evaluateKeyboardEvent` отображает его в `0x16` и вызывает
`preventDefault()`, поэтому Chromium не выполняет команду Paste. Настройка
`AreBrowserAcceleratorKeysEnabled` тут ни при чём — документация MS прямо перечисляет `Ctrl+X/C/V`
как неотключаемые ею. Вставку выполняет страница.

**`event.key` зависит от раскладки.** На ЙЦУКЕН физическая V даёт «м». Обходной путь вставки,
написанный ради русской раскладки, на ней же и не работал.

**`TerminalPump.DisposeAsync` отписывался от `Exited` первой строкой**, до закрытия псевдоконсоли:
код выхода уходил на страницу только по таймауту — **каждое закрытие вкладки стоило 2 секунды**.
Прогон тестов после правки: 10 с → ~1 с.

### Измеренное (аудит: БЮДЖЕТ СОБЛЮДЁН)

- Склейка вывода **1:127…1:167**: ConPTY отдаёт чтения по 129–130 байт, 8070 чтений на мегабайт;
  на страницу уходит 41 сообщение вместо восьми тысяч.
- Аллокации горячего пути 2,67 МиБ на МиБ вывода — ровно строка `out` (base64 ×4/3, UTF-16 ×2).
- Пауза GC 0,04 %; аккумулятор не растёт; backpressure держит 2 незавершённые записи при лимите 4
  и переживает потерю квитанций (каждая третья выброшена — 512 из 512 пачек доехали).
- Задержка ввода под шумом четырёх соседей не отличается от одиночной (p95 31,5 мс); пол задаётся
  двумя кадрами склейки по 16 мс.
- Самостоятельный выход оболочки освобождает псевдоконсоль за 58 мс без GC; закрытие 5 живых
  вкладок 19 мс вместо 10,07 с, 20 вкладок — 38 мс вместо 40,31 с.
- Переключение вкладки — **ровно одно сообщение** `show`, со страницы в C# ноль.
- 20 вкладок × 5 раундов: куча плато 7,9 МБ, RSS плато 47–48 МБ, осиротевших процессов 0.

## M2 — проекты и вкладки (закрыт)

`projects.json` с атомарной записью, панель проектов с веткой git, мультиплексор вкладок, запуск
`claude` командой в stdin, горячие клавиши. Три блока писались параллельно за контрактами.

### Правки по визуальной приёмке

Пользователь принял «в целом гуд» и назвал три дефекта — все исправлены:

1. **Всплывающая подсказка над терминалом.** Заголовок садился атрибутом `title` на
   `.terminal-host`, а тот растянут на всю область сессии (`position:absolute; inset:0`) —
   подсказка вылезала на любое движение мыши. Заменено на `data-terminal-title`.
2. **Системная полоса заголовка.** Своё обрамление по макету: `WindowStyle="None"` +
   `WindowChrome` (`CaptionHeight=38`, `ResizeBorderThickness=6`, `GlassFrameThickness=0`),
   свои кнопки через `SystemCommands`. Перетаскивание, двойной клик и прилипание к краям —
   системные, своего кода нет. Панель задач защищена хуком `WM_GETMINMAXINFO`
   в `WorkAreaPlacement.KeepMaximizedWithinWorkArea`.
3. **Вкладки всех проектов в одной куче.** Полоса показывает вкладки только выбранного проекта.

Попутно найден дефект, невидимый ни в сборке, ни в тестах: **дочерний HWND WebView2 накрывал края
окна, `WM_NCHITTEST` до окна не доходил, и окно перестало тянуться за правый и нижний край.**
Лечится отступом 6 px у области терминала.

Причина «переключение проекта не работает» оказалась не в клике: `Select` вызывался, но подсветка
считалась по формуле «проект активной вкладки, иначе выбранная строка». Теперь выбор единственный.

## M4 — состояние сессий (в работе)

**Готово (Sessions + Terminal):**

- `IHookSettingsProvider`: `hook-settings.json` и `hook-send.cmd` в `%APPDATA%\ClaudeAgentsShell`,
  хуки `SessionStart`, `Stop`, `SessionEnd`. Отправка через `curl.exe` (есть в Windows 10 1803+),
  обёртка `.cmd`, потому что хуки запускаются через `cmd /d /s /c`.
- `IHookListener`: `HttpListener` на `http://127.0.0.1:<свободный порт>/hook/`, только loopback.
  Отвечает `204` и закрывает ответ **до** подъёма события — медленный подписчик не задержит `claude`.
- `ISessionHistoryReader`: slug, потоковое чтение с остановом на первом сообщении, кэш
  по `(путь, mtime, размер)`.
- Токен вкладки — 128 бит из `RandomNumberGenerator`, уходит в окружение псевдоконсоли под
  `CLAUDE_AGENTS_SHELL_TOKEN`, возвращается заголовком `X-Agents-Shell-Token`.
  `ITerminalWorkspace.TryResolveTerminal` сопоставляет токен с вкладкой.
- `--settings` в команде запуска; при сбое создания файла — запуск без хуков, молча.

**Осталось (блок App):** точки состояния на вкладках, кликабельный счётчик «N ждёт ввода»,
заголовки вкладок из первого сообщения, переход «первый ввод после `Stop` → работает».

## M5 — доводка (не начат)

Готовы только порты и их реализации: `IShellLauncher` (открыть папку в проводнике),
`IFileProbe` (наличие `CLAUDE.md` и `.mcp.json`). Интерфейса нет ни у чего.

Не сделано: диалог настроек проекта, контекстные меню на строке проекта, перетаскивание вкладок,
понятное сообщение при отсутствии WebView2 Runtime (сейчас заглушка `MessageBox`).
Восстановление раскладки отложено пользователем.

Краевые случаи раздела 8 ТЗ закрыты попутно: исчезнувший каталог блокирует запуск; откат
на `powershell.exe`; упавший процесс оставляет вкладку с кодом выхода; битая строка `.jsonl`
пропускается; отсутствие каталога истории не ошибка; большая вставка не рвётся склейкой.

---

# Контракты

Меняются только оркестратором.

**Domain:** `TerminalId`, `TerminalSize`, `ShellKind`, `ShellStartCommand`, `PtyStartInfo`,
`SessionLaunch`, `TabState`, `ProjectDefinition`, `SessionSummary`, `HookEvent` + `HookKind`.

**Application.Ports:** `IPtySession` (+`IPtySessionFactory`, `PtyStartException`), `IShellProvider` /
`IShellResolver` (+`ShellNotFoundException`), `ITerminalBridge`
(+`TerminalBridgeUnavailableException`), `ITerminalWorkspace`, `IProjectStore`, `IAppDataPaths`,
`IGitBranchReader` / `IGitBranchWatcher`, `IDirectoryProbe`, `IFileProbe`, `ISessionCommandBuilder`,
`ISessionHistoryReader`, `IHookListener`, `IHookSettingsProvider`, `IShellLauncher`.

**Terminal.Protocol:** `InboundBridgeMessage`, `IBridgeMessageWriter`, `IBridgeMessageParser`,
`PendingWriteRegistry`, `TerminalOptions`.

Порты уровня оболочки живут в `App/Services`: `IFolderPicker`, `IUserPrompt`, `IUiDispatcher` —
это WPF-специфика, потребляемая только App.

## Протокол моста — фактическая форма

Из C# на страницу (5 типов раздела 3.2 ТЗ; у `out` добавлено поле `seq`):

    { "type": "out",    "id": "t1", "seq": 12, "b64": "<сырые байты PTY в base64>" }
    { "type": "create", "id": "t1", "title": "проект · каталог" }
    { "type": "show",   "id": "t1" }
    { "type": "close",  "id": "t1" }
    { "type": "exited", "id": "t1", "code": 0 }

Со страницы в C# (3 типа из ТЗ + служебный `ack`):

    { "type": "in",     "id": "t1", "b64": "<байты ввода>" }
    { "type": "resize", "id": "t1", "cols": 120, "rows": 34 }
    { "type": "ready",  "id": "t1" }
    { "type": "ack",    "id": "t1", "seq": 12 }

---

# Решения

## Отступления от ТЗ — все согласованы с пользователем

- **Полоса вкладок показывает вкладки только выбранного проекта.** Решение пользователя **поверх**
  разделов 6.2 и 6.3 ТЗ: там написано «клик по строке… если открытых нет — ничего не делает»
  и подразумевается одна общая полоса. **ТЗ не правлен — не «починить» обратно по нему.**
  Клик по строке проекта без вкладок даёт пустую полосу и заглушку с подсказкой: это видимый
  результат, а не молчание.
- **Горячие клавиши на `Ctrl+Shift`, а не `Ctrl+T`/`Ctrl+W` из раздела 6.3.** `Ctrl+T` (transpose)
  и `Ctrl+W` (kill-word) уходят в оболочку: оконные команды — `Ctrl+Shift+T`, `Ctrl+Shift+W`,
  `Ctrl+Tab`, `Ctrl+Shift+Tab`, `Ctrl+1..9`.
- **`Ctrl+V` вставляет** (а не шлёт `0x16`), `Ctrl+C` с выделением копирует, без выделения — `SIGINT`.
- **Два расширения протокола раздела 3.2**, оба вынужденные: служебный входящий `ack` и поле `seq`
  в `out`. Раздел 3.3 требует счётчик незавершённых `term.write`, но канала подтверждения
  не описывает; без номера пачки странице нечего вернуть, а встречный счётчик обнуляется при
  перезагрузке страницы.
- **M3 отложен**, восстановление раскладки из M5 — тоже.

## Техника

- **ConPTY напрямую через P/Invoke:** `Pty.Net` в NuGet существует только как `0.1.16-pre`.
  Раздел 2 ТЗ такой вариант прямо допускает.
- **xterm.js 5.5.0** + addon-fit 0.10.0, addon-webgl 0.18.0, addon-unicode11 0.8.0 — последние
  стабильные аддоны с peer-зависимостью на 5.x.
- **`System.GC.LOHThreshold = 196608`** в csproj приложения; константа 64 КБ из раздела 3.3
  сохранена. Точка перехода в LOH лежит ниже — ~31,8 КБ, — но доля пауз GC 0,04 %.
- **Пайпы анонимные, чтение блокирующее на потоке пула** — точное следование примеру Microsoft.
  Измерено: 5 и 20 припаркованных чтений не дают задержки постановки задач в пул.
- **Сравнение клавиш только по физической кнопке:** `Key` в C#, `event.code` на странице.
  `event.key` допустим лишь для `Enter`, `F5` и стрелок. Списки сочетаний продублированы
  в `ShellShortcutMap.TryMap` и `isWindowShortcut` (`web/app.js`), механической сверки нет —
  **при правке трогать оба.**
- **`AreBrowserAcceleratorKeysEnabled = false` обязательно:** выключение глушит reserved-класс
  Chromium (`Ctrl+W`, `Ctrl+N`, `Ctrl+T`), который иначе до страницы не доходит и не поддаётся
  `preventDefault`. Защита от перезагрузки — два рубежа: отмена навигации в C# и точечный
  `preventDefault` **без `stopPropagation`**, чтобы `Ctrl+R/F/P/G` доходили до readline.
- **Ресайз применяется ко всем вкладкам, включая скрытые,** через `term.resize`; `fit()` — только
  на видимой. Размер считается один раз по видимому контейнеру, дебаунс 80 мс.
- **Мгновенное переключение держится на хрупком инварианте:** `addon-fit` вычитает
  `viewport.scrollBarWidth`, а xterm 5.5 считает его один раз в конструкторе как
  `offsetWidth - offsetWidth || 15`. `term.open()` всегда зовётся на скрытом элементе, поэтому
  у всех терминалов один и тот же запасной `15`. Держится, пока терминал открывается **скрытым**
  и `scrollback != 0`. Открытие видимым заставит переключение перекладывать буферы всех вкладок.
- **Запуск командой в stdin:** ждём первого байта вывода из PTY, выдерживаем 250 мс, пишем строки
  байт в байт; по таймауту 5 с пишем всё равно. Разбора содержимого нет и быть не может —
  сигнала готовности readline ConPTY не даёт, а поиск приглашения запрещён разделом 7.
  **250 мс подобраны на глаз и проверяются только приёмкой.**
- **Завершитель строк stdin — `\r`, не `\n`:** LF оболочка за нажатие Enter не считает.
- **Первая запись `projects.json` делается `File.Move`:** `File.Replace`, названный в разделе 4.1,
  бросает при отсутствии файла назначения.
- **`slug` = замена всего, кроме `[a-zA-Z0-9]`, на дефис** — а не `char.IsLetterOrDigit`: второй
  вариант оставил бы кириллицу буквами и увёл к несуществующему каталогу.
- **Токен хука не выводится из идентификатора вкладки:** приёмник слушает обычный loopback-порт,
  доступный любому локальному процессу, а идентификатор вкладки уходит на страницу в каждом
  сообщении моста.
- **`--noproxy "*"` в команде хука обязателен:** с `HTTP_PROXY` в окружении вызов на loopback ушёл
  бы в прокси и маркеры пропали бы молча. Проверено вручную.
- **Хук обязан выходить с кодом 0 и молчать:** ненулевой код у `Stop` не даст агенту остановиться,
  а stdout у `SessionStart` уедет в контекст модели.
- **`ISessionCommandBuilder.Build` принимает `hookSettingsPath` параметром, а не конструктором:**
  файл готовится лениво и кэшируется в том числе при сбое, поэтому одна вкладка может получить
  его, а соседняя нет. Плюс построитель остаётся чистой функцией.

---

# Принятые замечания

- **`CancellationToken` не отменяет уже начатое чтение из PTY** — следствие синхронных анонимных
  пайпов. Корректность освобождения держится на таймаутах. Уход на `FILE_FLAG_OVERLAPPED`
  измерением не оправдан.
- **`0x16` (quoted-insert) через `Ctrl+V` не ввести** — цена решения «`Ctrl+V` вставляет».
- **`Ctrl+C` с выделением копирует:** случайное выделение мышью молча превратит попытку прервать
  агента в копирование.
- **Строка `out` попадает в LOH при пачке от ~31,8 КБ**; ~15 % пачек перешагивают. Ручка —
  `FlushThresholdBytes`, но доля пауз GC 0,04 %.
- **Предел незавершённых записей фактически `MaxPendingWrites + 1`:** сброс по таймеру ставит
  пачку, не сверяясь со счётчиком.
- **Контекст WebGL не освобождается детерминированно.** В `addon-webgl` 0.18 нет ни одного вызова
  `loseContext`: `dispose()` снимает канвасы, а контекст забирает сборщик мусора Chromium.
  Риск лимита контекстов закрыт **обработчиком `onContextLoss`** — на ~17-й вкладке Chromium
  гасит самый старый контекст, и та вкладка переезжает на запасной рендерер (в xterm 5.5 — **DOM**,
  не canvas).
- **При смене DPI размер знакоместа меняется без изменения контейнера,** `ResizeObserver`
  не срабатывает, пересчёта `cols`/`rows` не происходит.
- **`WebView2TerminalBridge` и клавиатурный слой страницы не покрыты автотестами** — `App` тянет
  WPF и `Dispatcher`, GUI в контейнере не запускается.
- **Полоса вкладок при шести и более вкладках:** скроллбар скрыт, кнопка «+» уезжает за край.
- **Первый замер размера ограничен 10 попытками по 80 мс** — если на холодном старте рендерер
  просыпается дольше, первая вкладка останется на размере по умолчанию до первого ресайза.
- **Копия `projects.json` чужой версии кладётся в `.bak`, но приложение об этом не сообщает.**
- **Счётчик «N ждёт ввода» считает все вкладки, а не только видимые** — смысл в том, чтобы
  заметить сессию в другом проекте. Значит в M4 клик по счётчику обязан заодно переключать
  выбранный проект.
- **Отсутствие `curl.exe` или неотвечающий приёмник** — хук молча ничего не делает, вкладка живёт
  без маркера. Штатная деградация раздела 5.3, диагностики намеренно нет.
- **Полоса фона 6 px справа и снизу** от отступа области терминала — видна и в развёрнутом окне,
  где резайз-борд не нужен.
- **Автоскрываемая панель задач** не всплывёт по наведению у развёрнутого окна: оно ровно
  по рабочей области. Лечится отступом в 1 px, не делали.

---

# План приёмки

## Что уже принято пользователем

**M1:** TUI Claude Code, рамки, кириллица, ширина эмодзи, ресайз, многострочная вставка,
`Shift+Enter`, `Ctrl+V`, `F5`, размер окна, `Ctrl+W` в оболочке, двойной клик по крестику.

**M2, первый проход:** «в целом гуд» — переключение вкладок, панель проектов, запуск сессий.
Три дефекта названы и исправлены.

## Приёмка правок M2 — проверить сейчас

Порядок важен: пункты 1–2 проверяют то, что тестами не закрывается вообще.

1. **Развернуть окно на мониторе с панелью задач** — нижний край не уходит под неё.
   Если есть второй монитор с другим масштабом, повторить на нём.
2. **Потянуть окно за правый и за нижний край** — тянется (был дефект: дочернее окно WebView2
   перехватывало мышь). И **ужать окно** — должно остановиться на 900×560, а не ужиматься
   до системного минимума.
3. **Поводить мышью над областью терминала** — всплывающей подсказки нет. На кнопке вкладки
   подсказка, наоборот, должна остаться.
4. **Кнопки обрамления** — свернуть, развернуть, восстановить, закрыть; двойной клик по полосе
   заголовка разворачивает и возвращает; окно перетаскивается за полосу.
5. **Два проекта, в одном две сессии, во втором ноль.** Клик по второму проекту: полоса пустеет,
   появляется заглушка с подсказкой, подсветка строки переезжает. Клик обратно: вкладки на месте,
   история в них не потерялась.
6. **`Ctrl+Tab` и `Ctrl+1..9`** ходят внутри выбранного проекта, а не по всем вкладкам.
7. **Пять сессий по трём проектам:** команда запуска доехала целиком в каждой; переключение
   мгновенное; счётчики сессий на строках верные.
8. **Дважды кликнуть по крестику** — окно закрывается один раз, в диспетчере не осталось
   `powershell.exe` и `conhost.exe`.

## Приёмка M4 — когда будет готов интерфейс состояний

1. **Точка состояния на вкладке.** Задать агенту вопрос: точка «работает». Агент закончил ответ:
   в пределах секунды точка меняется на «ждёт ввода» — **в том числе в фоновой вкладке и
   в вкладке невыбранного проекта**.
2. **Первый ввод после ответа** возвращает точку в «работает».
3. **Счётчик «N ждёт ввода»** считает все проекты. Клик по нему переключает на первую такую
   вкладку — **и заодно на её проект**, иначе вкладка останется невидимой.
4. **Заголовок вкладки** после первого сообщения меняется с «новая сессия» на короткое имя
   из этого сообщения.
5. **Деградация без хуков.** Запустить `claude` руками внутри вкладки (не кнопкой приложения):
   вкладка живёт без маркера состояния, ошибок не показывается. Это штатное поведение.
6. **Пять сессий одновременно:** маркеры не путаются между вкладками. Это проверка того, что
   токен вкладки доезжает и сопоставляется верно.

## Приёмка M5 — когда будет готов

1. Диалог настроек проекта: путь с выбором папки, имя, оболочка, команда перед запуском,
   дополнительные аргументы; показывает, найдены ли `CLAUDE.md` и `.mcp.json`.
2. Контекстное меню на строке проекта: открыть папку в проводнике, настройки, убрать из списка.
3. Перетаскивание вкладок меняет порядок.
4. Краевые случаи раздела 8 ТЗ, которые видны глазами: удалить каталог проекта при живой сессии;
   указать проект без `claude` в PATH; убить процесс сессии извне (`taskkill`) — вкладка остаётся
   с пометкой о коде выхода и кнопкой «перезапустить».

## Что глазами не ловится — знать при приёмке

- **Смена DPI без изменения размера окна** (перенос между мониторами 100 % и 150 %):
  `cols`/`rows` остаются от прежнего масштаба. Смотреть на правый край рамок TUI.
- **Хук не доехал** выглядит точно так же, как «агент ещё работает»: точка просто не меняется.
  Если маркеры не появляются совсем — причина в файле настроек, `curl.exe` или прокси,
  а не в самом состоянии.
- **Обрыв квитанций** выглядит как «вывод замер насовсем» после первых ~4 пачек.
- **Замороженная ветка git** после ошибки наблюдателя неотличима от «ветка не менялась».

## Открытые вопросы к пользователю

Пока нет.
